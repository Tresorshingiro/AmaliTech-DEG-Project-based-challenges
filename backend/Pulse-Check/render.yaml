services:
  - type: web
    name: pulse-check-api
    runtime: java
    rootDir: backend/Pulse-Check
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/pulse-check-0.0.1-SNAPSHOT.jar
    plan: free
    envVars:
      - key: JAVA_VERSION
        value: 17
